class MioNetwork {
    constructor(settings) {
        // Inputs
        this.inputLabels = settings.inputs;
        this.inputCount = this.inputLabels.length;
        this.inputs = new Array(this.inputCount);
        // Outputs
        this.outputLabels = settings.outputs;
        this.outputCount = this.outputLabels.length;
        this.outputs = new Array(this.outputCount);
        // Modifiers
        
    }
}